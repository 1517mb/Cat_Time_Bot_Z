version: '3.8'

services:
  cat_bot:
    build: .
    container_name: cat_time_bot_z
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./sqlite.db:/app/sqlite.db
      - ./old_bot_database.db:/app/old_bot_database.db
      - ./logs:/app/logs